
import { IssueStatus, IssuePriority } from './types';

export const STATUS_OPTIONS = Object.values(IssueStatus);
export const PRIORITY_OPTIONS = Object.values(IssuePriority);
export const PAGE_SIZE_OPTIONS = [10, 25, 50];
